import styled from 'styled-components';

export const ButtonWrapper = styled.button`
display: flex;
  width: 28px;
  height: 28px;
  &:hover {
    background: rgba(138, 139, 157, 0.4);
    border-radius: 2px;
  }
  button {
    margin: auto;
  }
`;
